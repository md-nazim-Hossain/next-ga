import React from "react";
import Head from "next/head";

type Props = {};

const about = (props: Props) => {
  return (
    <div>
      <Head>
        <title>about</title>
      </Head>
      <h1>About Pages</h1>
    </div>
  );
};

export default about;
